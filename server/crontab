# TAPD -> 企微智能表格 同步定时任务（容器内 cron，时区 Asia/Shanghai）
# 格式：分 时 日 月 周 用户  命令
# 默认每天 22:10 执行；如需改时间，修改此处后重新 build 镜像即可。

# 每分钟心跳：确认 cron 服务存活（直接写容器 stdout，Rainbond 日志可见）
* * * * * root echo "[heartbeat] $(date '+%F %T') cron alive" >> /proc/1/fd/1 2>&1

# 验证期：每 5 分钟跑一次同步（不再需要到 22:10 等待，立即能验证）
# 验证成功、表格有数据后，再改回 "0 22 * * *" 重新 build 即恢复每天执行
*/5 * * * * root /bin/bash /app/server/sync_runner.sh >> /app/data/cron.log 2>&1
